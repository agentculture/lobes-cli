# Copy to .env (gitignored) and adjust. Consumed by docker-compose.yml.

# HuggingFace token. Required only if the model repo is gated — accept its
# license on huggingface.co first, then paste a read token here.
HF_TOKEN=

# Model to download/serve, and the name clients address it by. VLLM_SERVED_NAME
# MUST equal the part after `vllm-local/` in culture.yaml's `model:` field, or
# the acp `vllm-local` provider won't resolve the model.
VLLM_MODEL=nvidia/Qwen3-32B-NVFP4
VLLM_SERVED_NAME=nvidia/Qwen3-32B-NVFP4

# Host port for the OpenAI-compatible API.
VLLM_PORT=8000

# Context window. Qwen3-32B is 32768 native; up to 131072 with YaRN rope scaling
# (also pass a matching --rope-scaling config when raising this).
VLLM_MAX_MODEL_LEN=32768

# Fraction of the 128 GB unified memory vLLM may reserve. Lower leaves headroom
# for the OS and other mesh agents; raise toward 0.85 on a dedicated box.
VLLM_GPU_MEM_UTIL=0.6
