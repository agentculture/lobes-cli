# The DGX Spark half of a mesh-brain deployment (brain-shapes t1, issue #113):
# keeps the Qwen `cortex` primary + the two pooling gears + the audio overlay
# on this box, and drops the Gemma `senses` lobe to a peer box (today, the
# Jetson AGX Thor running thor-lobe.toml) instead of co-residing it here.
#
# Differs from machine-as-brain.toml ONLY in `hosts` (senses dropped) and, from
# a later task (t2) onward, `overrides` (t2 re-derives cortex's budget upward
# now that it no longer shares the box with senses -- co-residency today caps
# cortex at util 0.30 / 131072 tokens instead of its 256K solo-native context,
# see CLAUDE.md's "Colleague roles"). This task (t1) leaves `overrides` empty
# -- the budget-override FIELDS exist in the schema (RoleProfile, reused
# verbatim), but no re-derived value is filled in yet.
name = "spark-lobe"
summary = "DGX Spark mesh-lobe -- Qwen cortex + embedder + reranker + audio, no Gemma senses"

hosts = ["cortex", "embedder", "reranker", "stt", "tts"]
