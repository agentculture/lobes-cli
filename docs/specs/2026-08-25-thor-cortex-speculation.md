# thor cortex speculation

> the Thor cortex gets speculative decoding back — DSpark or MTP, behind the Triton GDN decode path
> instruction: Run three arms on the physical Thor at `max_model_len`=262144, `gpu_mem_util`=0.58, `kv_cache_dtype`=auto, `hf_overrides` UNCHANGED, `VLLM_GDN_DECODE_KERNEL`=triton set on the vllm-primary service for all three: (A) control, `PRIMARY_SPECULATIVE_CONFIG`= (empty, flag omitted); (B) MTP-n2, the compose template default; (C) DSpark block-7 with the pinned RadixArk revision, as spark-lobe.toml renders it. Prove each arm's argv from 'docker inspect model-gear-vllm-primary', never from .env. Reuse the Spark spike's three content shapes and prompts so the tables are comparable.

## Audience

- the lobes operator running the physical Jetson AGX Thor (`sm_110`) cortex, plus any future `sm_110` board this fleet adds — and, second-hand, every mesh caller addressing model=cortex on this box, whose decode rate and served `max_model_len` both change
  - instruction: Announce the window change to mesh consumers before deploying it — a caller relying on >262144 tokens breaks silently otherwise. Check the Orin's cortex referral to this box still resolves after the change.

## Before → After

- After: the Thor cortex serves Qwen3.8-27B at `max_model_len`=262144 with a speculative drafter chosen by measurement, and the choice — including 'neither, keep no-speculation' — is backed by a dated docs/evidence transcript covering a triton no-spec control, MTP-n2, and DSpark, each across code/reasoning/prose. thor.toml's flat 'MTP MUST BE OFF' comment is replaced by the measured condition under which speculation does or does not work on `sm_110`
  - instruction: Update lobes/profiles/builtin/thor.toml's cortex comment in the SAME PR as the transcript, never a follow-up. If a thor-cortex shape is the chosen home for the config, note it against the parked #204 question rather than silently forking spark-lobe.

## Why it matters

- the measurement itself is the win, independent of the outcome. Today the Thor's cortex — the fleet's reasoning/deciding/final-authority lobe — runs with NO speculative decoding at all because d1 recorded 'MTP MUST BE OFF on `sm_110`' as a flat kernel-gap fact. Whether speculation turns out to be worth deploying or not, this run replaces that flat fact with a measured, conditioned answer, and gives the repo its first `sm_110` evidence for the Triton GDN decode path
  - instruction: Write the transcript under docs/evidence/ even for a negative result, and update lobes/profiles/builtin/thor.toml's cortex comment (currently a flat 'MTP MUST BE OFF' at lines 35-39) to state the measured condition instead. Cite the transcript from docs/dspark-speculation.md and docs/qwen3.8-27b-nvfp4.md.

## Requirements

- `VLLM_GDN_DECODE_KERNEL`=triton is the unlock, and it is a first-class vLLM env var (envs.py:131 and :1211, Literal\['cuda','triton'\], default 'cuda'). `qwen_gdn_linear_attn.py`:491-505 reads it, and :1807 makes '`gdn_decode_kernel` == cuda' a REQUIRED conjunct of the fused-MTP gate — so setting triton routes the target's GDN layers down `_forward_core` (the Triton spec path) and the missing-image kernel is never launched. This turns the d1 'MTP MUST BE OFF on `sm_110`' blocker from a hard wall into a config knob, and it applies to MTP-n2 and DSpark alike
  - instruction: Set `VLLM_GDN_DECODE_KERNEL`=triton in the vllm-primary service environment (not just .env — confirm it lands via 'docker inspect model-gear-vllm-primary --format {{json .Config.Env}}'). Capture the 'GDN decode kernel: triton' `info_once` line from the container logs as proof the value was read.
  - honesty: setting `VLLM_GDN_DECODE_KERNEL`=triton is sufficient on its own — no source patch, no image change. Verified by the arm-B boot completing a multi-token decode with --speculative-config present. Falsified if the boot still crashes, or if vLLM raises '`VLLM_GDN_DECODE_KERNEL`=cuda is not supported' style config errors that reveal a second required knob
- the vLLM build already on the Thor declares the method: probing vllm.config.speculative.SpeculativeMethod inside the running model-gear-vllm-primary lists 'dspark' (and 'dflash'), on the identical build string (0.26.1rc1.dev942+g5a4c8d992) the Spark's 2026-08-24 spike ran. No image change is needed to try this — only argv and one env var
  - instruction: Do not change the pinned image digest for this spike. If DSpark will not load on 8bd082, stop and report rather than bumping the nightly — an image change would confound every arm against the 2026-08-20 baseline.
  - honesty: no image bump is needed. Verified by the DSpark arm loading DSparkDraftModel on the Thor's currently pinned digest 8bd082c274fa. Falsified if the drafter fails to resolve, in which case the spike scope grows to an image change and should stop for a decision
- the Thor's deployed compose predates the `PRIMARY_SPECULATIVE_CONFIG` slot: grep of ~/.lobes/docker-compose.yml finds only `MULTIMODAL_SPECULATIVE_CONFIG` expanded, and the .env carries no `PRIMARY_SPECULATIVE_CONFIG` line — MTP is off there by a hand-edit that deleted the hardcoded flag, exactly the deployment defect docs/dspark-speculation.md point 7 found on the Spark. So arming any drafter on the Thor requires re-rendering the compose first, which is the same operation that carries the spark-lobe trap above. Sequence them deliberately
  - instruction: Re-render the compose from the current wheel first, as its own step, and verify the ... slot is present before attempting any arm. Snapshot ~/.lobes before touching it, as d1 did.
  - honesty: the deployed compose lacks the `PRIMARY_SPECULATIVE_CONFIG` slot. Already verified this session by grepping ~/.lobes/docker-compose.yml (only `MULTIMODAL_SPECULATIVE_CONFIG` expands) and the live argv (no --speculative-config). Falsified only if a later re-render happens before the spike starts
- arm order on the Thor is control -> MTP-n2 -> DSpark, all at 262144, all behind `VLLM_GDN_DECODE_KERNEL`=triton, all three content shapes (code/reasoning/prose), reported per shape. The control arm is triton + NO speculation, re-measured same-day: without it the 12.1 tok/s figure from 2026-08-20 is a cuda-path number and cannot serve as the baseline for a triton-path arm (docs/model-switch-playbook.md's same-day/same-harness rule). Deltas at or under ~15% are noise per the Spark spike's own variance floor
  - instruction: Order: (A) triton + no spec, (B) triton + MTP-n2, (C) triton + DSpark block-7. Do not reorder to put the exciting arm first. Record wall-clock timestamps per arm so a mid-run environment change is detectable afterwards. Follow the `drop_caches`-before-heavy-recreate boot discipline this box needs.
  - honesty: the control arm is genuinely comparable to the speculative arms — same day, same harness, same prompts, same box state. Verified by running all three arms back-to-back in one session with only `PRIMARY_SPECULATIVE_CONFIG` changing. Falsified if the box is rebooted, re-rendered, or the image changes mid-run, in which case the affected arms are re-measured, not reconciled

## Honesty conditions

- `VLLM_GDN_DECODE_KERNEL`=triton actually prevents the `fused_gdn_decode_post_conv_mtp` launch on `sm_110` under a real speculative batch. Verified by BOOTING the Thor cortex with triton set plus any --speculative-config and completing a multi-token decode — the d1 failure surfaced on the FIRST decode, so a healthy /health is not evidence. Falsified if the same 'no kernel image is available for execution on the device' RuntimeError appears, in which case the whole frame collapses to a negative result and is reported as one
- 262144 is genuinely the ceiling for a DSpark arm on this box. Verified by the DSpark arm booting at 262144 and, if headroom looks generous, one probe boot above it to record where vLLM actually refuses. Falsified if DSpark boots comfortably at a larger window, in which case the trade-down was over-conservative and the served contract should be revisited
- the Thor really does render spark-lobe. Verified by reading `LOBES_SHAPE` / the shape provenance in ~/.lobes/.env and by a dry-run 'lobes init --shape spark-lobe --profile thor' diffed against the deployed compose. Falsified if the Thor renders something else, in which case this boundary is withdrawn
- the run produces a durable, citable artifact regardless of outcome — a dated docs/evidence transcript under this repo's measured-here labelling, with conditions attached per docs/measuring-lane-performance.md rule 3. Falsified if the spike ends with only a verbal 'it didn't work' and no transcript
- the served contract change is actually visible to callers. Verified by 'lobes capabilities' / GET /capabilities on the Thor reporting cortex context 262144 rather than 1048576 after the change. Falsified if the surfaces still advertise 1M, which would be a capabilities bug worth its own issue
- thor.toml's cortex comment ends up stating a measured condition, not a flat prohibition. Verified by reading lines 35-39 after the change and finding the Triton knob named. Falsified if the spike lands numbers but leaves the profile comment untouched, which would recreate the exact drift this frame exists to fix
- a negative result is written up with the same care as a positive one. Verified by the transcript existing and being cited from docs/dspark-speculation.md regardless of outcome. Falsified if a failed unlock produces no evidence file — that is the specific failure mode c16 exists to prevent
- an observed output divergence is reported IMMEDIATELY in the transcript and then investigated to a named cause — implementation defect or algorithmic divergence — with the losslessness claim SUSPENDED, not asserted, in the interim. Falsified by either failure mode: silently defending losslessness while a divergence sits unexplained, OR sitting on the observation until the investigation concludes. The investigation is bounded: if no cause is named, the claim is retracted rather than left suspended indefinitely

## Success signals

- a docs/evidence/<date>-\*-thor.txt transcript exists carrying: three arms x three content shapes with per-shape tok/s and acceptance, argv proven from docker inspect rather than .env, the same-day triton no-spec control, and an explicit statement of which arm was adopted and which rejected. A NEGATIVE result — the Triton path does not unlock speculation, or unlocks it at a net loss — satisfies this signal exactly as well as a positive one
  - instruction: Name the transcript docs/evidence/<date>-spike-thor-cortex-speculation.txt (spike, not accept) unless an arm is actually adopted, in which case add a separate accept- transcript. Follow the labelling in docs/measuring-lane-performance.md rule 3: every number carries its conditions.

## Scope / boundaries

- the 1M YaRN window and DSpark cannot both survive on the Thor. The Spark refused the DSpark boot at `max_model_len`=1048576 / util 0.58 ('needs 51.47 GiB KV; available 40.76'), and the Thor is strictly worse per GiB of KV: it runs `kv_cache_dtype`=auto (the validated #109 divergence) where the Spark runs fp8, and its 1M pool is 1,114,504 tokens vs the Spark's 1,271,476 at the same util. So any Thor speculation arm inherits the same forced trade-down to the checkpoint's native 262144 — a served-contract change visible through lobes capabilities, not a private dial
  - instruction: Record the KV pool size and concurrency multiple vLLM reports for each arm, so the served-contract cost of the drafter is measured on this box rather than inherited from the Spark's numbers.
- PR #202 has ALREADY changed what the Thor would render, and nobody measured it on `sm_110`. The d1 transcript records the Thor deployment as 'lobes init --shape spark-lobe --apply --force on the thor card', so the Thor borrows spark-lobe — the very shape 830bbf3 moved to DSpark @ 262144. A routine re-render on the Thor today would arm DSpark argv on a box where the fused GDN MTP kernel has no image, AND silently drop the served window from 1M to 262144. This is the spark-lobe re-render trap that docs/dspark-speculation.md point 8 closed for the Spark, still open in the opposite direction for the Thor
  - instruction: Before any --apply on this box, run the re-render DRY (no --apply) and diff the proposed argv against 'docker inspect'. Never let the speculation spike be the first thing that also silently changes the served window.
- no quality claim rides on this, and an observed divergence triggers INVESTIGATION before any verdict. Block speculation is lossless BY CONSTRUCTION (the target verifies every drafted token) — docs/dspark-speculation.md labels that STRUCTURAL, not measured, and explicitly notes it is 'not a claim that any particular implementation is bug-free: a broken verification path, a mangled config, or a silently-disabled speculative flag can all violate it in practice'. A Triton-vs-CUDA GDN decode swap is a further axis again, touching the non-speculative path too. So this spike measures speed and acceptance only; if an output difference IS observed on a speculative arm, the correct first move is to determine whether it is an implementation defect (config, verification path, parser) or a genuine algorithmic divergence — the structural claim is about the algorithm, and a defect falsifies the deployment, not the algorithm
  - instruction: Run the d1 probe suite (`known_answer`, `tool_calls`, gateway smoke) on EVERY arm, not just the fastest. If outputs differ, record the raw divergence in the transcript before diagnosing it, then check the cheap implementation causes first — argv via docker inspect, sampling parameters, the `qwen3_coder_thinking` parser — before reaching for an algorithmic explanation.

## Non-goals

- do not chase the published 34-38 tok/s DGX Spark figure on the Thor. It is PUBLISHED-ELSEWHERE — a different publisher's checkpoint (RadixArk NVFP4), a different engine (SGLang), a different target quantization, on different silicon (`sm_121`, not `sm_110`). docs/dspark-speculation.md already records that this fleet measured its own numbers rather than closing that gap, and a Thor run widens the gap rather than narrowing it
- a single-shape throughput number is not a description of a speculative lane. The Spark spike measured a ~3x content spread (DSpark 46.20 code / 31.73 reasoning / 13.71 prose tok/s at 262144) and recorded ~10-13% single-run variance, so any Thor arm must run at least the same three content shapes, label every number with the shape that produced it, and treat deltas at or under ~15% as noise

## Assumptions

- the Thor's 12.1 tok/s cortex is the NO-SPECULATION floor, not an MTP-assisted rate: docs/evidence/2026-08-20-accept-cortex-local-thor.txt records MTP OFF, and the live 'docker inspect model-gear-vllm-primary' argv today carries no --speculative-config at all. The Spark's own no-spec floor measured 9.93-11.42 tok/s, so 12.1 is the same regime — the prize here is a ~4x speculation multiplier, not the Spark's +64% MTP-to-DSpark delta
- vLLM's own auto-fallback cannot detect this failure: `_fused_gdn_decode_unsupported_reason` (`qwen_gdn_linear_attn.py`:511-533) tests gqa layout, head dims, dtypes, `has_device_capability`(80), and hasattr(torch.ops.`_C`,'`fused_gdn_decode_post_conv_mtp`'). On `sm_110` the symbol IS built and capability IS >= 8.0 — the wheel just ships no `sm_110` SASS image for it — so every guard passes and the failure surfaces at kernel launch. That is why d1 saw a crash on first decode instead of a graceful Triton fallback, and it matches the cuda-wheel-arch lesson (Spark `sm_121` vs Thor `sm_110` are not one arch)
- MTP-n2 and DSpark have IDENTICAL `sm_110` exposure — a hypothesis I formed and then REFUTED by reading the source, recorded so it is not re-argued. I expected the checkpoint's own MTP head to run GDN layers (making MTP the riskier arm on `sm_110`) and DSpark's separate dense drafter to be cleaner. It is not so: `qwen3_5_mtp.py`:118-124 builds the MTP head's decoder layers with `layer_type`='`full_attention`', and `qwen3_dspark.py`'s drafter is likewise full-attention GQA. NEITHER drafter touches GDN. The only `fused_gdn_decode_post_conv_mtp` exposure is the TARGET's verification batch, which `VLLM_GDN_DECODE_KERNEL`=triton covers identically for both. Drafter choice is therefore a pure throughput question on this box, not a kernel-risk question
- the fused-GDN-MTP kernel gap is NOT sidestepped by switching drafter — a SOURCE-READ PREDICTION, never measured, and moot in the shipped design since all three arms run behind the Triton knob regardless. Read in the Thor's OWN running container (vllm 0.26.1rc1.dev942+g5a4c8d992): `qwen_gdn_linear_attn.py`:1798 `_can_use_fused_gdn_mtp_decode` gates on '`spec_sequence_masks` is not None and `num_decodes` == 0 and `num_spec_decodes` > 0' — a property of the TARGET's verification batch, not of which drafter produced it. DSpark's block-7 verification width 8 is <= `MAX_FUSED_GDN_MTP_TOKENS` (8), so on the CUDA path DSpark would launch the same `fused_gdn_decode_post_conv_mtp` kernel that killed the d1 first decode. Recorded so nobody re-proposes 'just use DSpark' as a way around the `sm_110` gap

## Scope exploration

- `s1` — `docs/evidence/2026-08-20-accept-cortex-local-thor.txt + live docker inspect on this box`: Thor cortex = Qwen3.8-27B, 1M YaRN, util 0.58, KV pool 1,114,504 tokens (1.06x), 12.1 tok/s, and NO speculative-config in the deployed argv — the baseline is the unspeculated floor
  - seeds: `c2`
- `s2` — `vllm model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py (read inside model-gear-vllm-primary)`: the fused GDN MTP decode gate keys off `num_spec_decodes` > 0, not off the speculative method — so adopting DSpark alone does NOT dodge the `sm_110` kernel gap
  - seeds: `c3` (rejected)
- `s3` — `vllm envs.py VLLM_GDN_DECODE_KERNEL + qwen_gdn_linear_attn.py:491-505,1798-1812`: a supported cuda|triton env var forces the non-fused GDN decode path and falsifies the fused-MTP gate — the candidate unlock for ANY speculation on `sm_110`
  - seeds: `c4`
- `s4` — `qwen_gdn_linear_attn.py:511-533 _fused_gdn_decode_unsupported_reason`: the built-in capability probe checks symbol presence and cc>=8.0, neither of which detects a missing `sm_110` SASS image — so the Triton fallback must be forced by env var, it will never trigger itself here
  - seeds: `c5`
- `s5` — `vllm.config.speculative.SpeculativeMethod, probed live in the Thor container`: 'dspark' is declarable on the Thor's current pinned nightly, same build string as the Spark spike — the spike is config-only, no image bump
  - seeds: `c6`
- `s6` — `spark-lobe.toml d4 comment block + the two 1M acceptance transcripts (spark 2026-08-19, thor 2026-08-20)`: DSpark's 1.36B drafter (2.53 GiB weights + 5 full-attention KV layers) forced 1M -> 262144 on the Spark; the Thor's non-fp8 KV cache makes it a harder fit, not an easier one
  - seeds: `c7`
- `s7` — `lobes/profiles/builtin_shapes/spark-lobe.toml vs docs/evidence/2026-08-20-accept-cortex-local-thor.txt`: the Thor renders spark-lobe, so the 2026-08-25 DSpark adoption silently became the Thor's declared config too — unvalidated on `sm_110`, and it also withdraws the Thor's measured 1M window
  - seeds: `c8`
- `s8` — `~/.lobes/docker-compose.yml and ~/.lobes/.env on this box`: the deployed Thor scaffold has no  slot — the documented off/retarget knob is dead there, so a spike needs a re-render or a direct compose edit, and .env alone proves nothing (verify from docker inspect)
  - seeds: `c9`
- `s9` — `docs/dspark-speculation.md 'Lossless by construction' section`: losslessness is a structural property cited, never measured here; the honesty condition is retract-on-observation — carry it unchanged into any Thor arm
  - seeds: `c10` (rejected)
- `s10` — `docs/dspark-speculation.md 'The published recipe' + provenance-label table`: the 34-38 tok/s headline is a citation under a PUBLISHED-ELSEWHERE label, not a target — the Thor arm reports its own measured-here numbers with conditions attached (rule 3)
  - seeds: `c11`
- `s11` — `docs/evidence/2026-08-24-spike-dspark-cortex-spark.txt three-arm three-shape table + variance note (point 6)`: content shape drives acceptance and therefore throughput; the harness and the reporting bar are already set by the Spark spike and should be reused verbatim on the Thor
  - seeds: `c12`
- `s12` — `qwen_gdn_linear_attn.py _forward_core (Triton path) vs _forward_core_decode_spec_fused_norm`: the Triton path is the general fallback for prefill and mixed batches and is exercised on the Thor today for prefill, but its decode-time cost on `sm_110` has never been isolated — needs a control arm
- `s13` — `lobes/profiles/schema.py:136 + builtin/thor.toml:19-53 + builtin_shapes/orin-cortex.toml`: the card can now express a draft opinion but cannot win against a shape override (#204); orin-cortex shows a card-specific cortex shape is an accepted in-tree pattern, and no thor-cortex exists
- `s14` — `vllm model_executor/models/qwen3_5_mtp.py:118-124 and qwen3_dspark.py (read in the Thor container)`: both drafters are full-attention; neither runs GDN. `sm_110` kernel risk is identical across MTP-n2 and DSpark, so it cannot be used to prefer either
  - seeds: `c13`
- `s15` — `docs/model-switch-playbook.md same-day baseline rule + dspark-speculation.md point 6 variance floor`: the 2026-08-20 12.1 tok/s baseline was taken on the CUDA GDN path; a triton-path arm needs its own same-day no-spec control or every delta is confounded by the kernel swap
  - seeds: `c14`
- `s16` — `spark-lobe.toml hf_overrides KEPT rationale + docs/evidence/2026-08-19-accept-qwen38-1m-spark.txt QA pass`: YaRN-vs-stock-rope at 262144 is a separate, unmeasured axis; the one quality measurement that exists weakly contradicts the premise that native rope is better, so it must not ride along in the speculation spike

## Decisions

- measure BOTH best candidates — MTP-n2 and DSpark block-7 — at 262144 behind the Triton GDN decode path, then choose the deployed default from the Thor's own numbers. Neither is pre-selected as the winner; MTP-n2 runs first only because it isolates the unlock from the drafter change

## Open parks

- [unknown_nonblocking] whether the thor card should stop borrowing spark-lobe. lobes/profiles/schema.py:136 `SPECULATIVE_CONFIG_ROLES` now includes cortex, so builtin/thor.toml COULD finally declare `speculative_config`='' and encode the `sm_110` MTP-off fact that today lives only as a prose comment (thor.toml:35-39) plus a hand-edit on the box. But a shape override composes ON TOP of the card, so spark-lobe's DSpark value would beat it — the exact precedence gap issue #204 tracks. The alternative is a thor-cortex shape, for which orin-cortex.toml is the existing precedent
- [unknown_nonblocking] whether choosing 262144 should ALSO retire the YaRN `hf_overrides` block. The operator's stated reason for 262144 is model quality at the native ceiling, which argues for stock rope too — but spark-lobe.toml keeps `hf_overrides` deliberately because every DSpark arm was measured with it in force, and the 2026-08-19 QA pass found zero quality cost from always-on YaRN (7/8 native vs 7/8 YaRN, identical failure — 8 prompts, thin). Retiring it needs its own transcript at 262144 + stock rope; do not fold it into the speculation spike as a second uncontrolled variable

## Resolved vagueness

- [unknown_blocking] the cost of `VLLM_GDN_DECODE_KERNEL`=triton on the NON-speculative decode path is unmeasured. The fused CUDA kernel exists because it is faster; forcing Triton may slow the unspeculated floor below 12.1 tok/s, so a triton+no-spec control arm is mandatory before any speculation number can be read as a net win — resolved: resolved by design, not by measurement: the spike's FIRST arm is a triton + no-speculation control, run same-day on the same harness (claim c14). That makes the Triton path's cost on the unspeculated decode a MEASURED OUTPUT of this work rather than an unknown blocking it. If the control comes in materially below the 2026-08-20 cuda-path 12.1 tok/s, that is itself a reportable finding and may end the spike with 'keep the cuda path, no speculation' — a valid outcome under c16
