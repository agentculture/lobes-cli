# Implementation Split Plan — thor worker arm: Qwen3.6-35B-A3B recipes

slug: `thor-worker-arm-qwen3-6-35b-a3b-recipes` · generated by `assign-to-workforce.sh split-plan --write` from `devague plan waves --json` (issue #82). Re-running this command overwrites this file in place; edits made to the **Task assignments** table below survive a regeneration, matched by task id.

## Dependency waves

- Wave 1: `t1`, `t3`, `t5`, `t6`, `t7`, `t8`, `t10`
- Wave 2: `t2`, `t9`
- Wave 3: `t4`, `t11`
- Wave 4: `t12`, `t15`
- Wave 5: `t13`
- Wave 6: `t14`

## Task assignments

Edit the Owner/Model columns before approving gate 2 — the default Model is a presentation-only proposal (`sonnet`), never a recommendation. Re-running `split-plan --write` preserves your edits here, matched by task id.

| Task | Owner | Model |
| --- | --- | --- |
| `t1` |  | sonnet |
| `t3` |  | sonnet |
| `t5` |  | sonnet |
| `t6` |  | sonnet |
| `t7` |  | sonnet |
| `t8` |  | sonnet |
| `t10` |  | sonnet |
| `t2` |  | sonnet |
| `t9` |  | sonnet |
| `t4` |  | sonnet |
| `t11` |  | sonnet |
| `t12` |  | sonnet |
| `t15` |  | sonnet |
| `t13` |  | sonnet |
| `t14` |  | sonnet |

## Wave 1

### t1 — Catalog: add nvidia/Qwen3.6-35B-A3B-NVFP4, demote Nemotron off the worker seat

- instruction: Fetch the checkpoint's config.json and `hf_quant_config.json` at implementation time and record what they say; today's read shows modelopt `MIXED_PRECISION` (experts `W4A16_NVFP4`, FP8 `linear_attn`, FP8 KV declared), 262144 native, `vision_config` present, `mtp_num_hidden_layers` present with `exclude_modules` \['mtp.layers.0\*','mtp\*'\]. Do not copy those values from the spec — re-read them.
- covers: c2, h2
- acceptance:
  - lobes/catalog.py holds a SupportedModel for nvidia/Qwen3.6-35B-A3B-NVFP4 whose `native_max_model_len`, quantization, `moe_backend` and `speculative_config` were read from the checkpoint's own config.json + `hf_quant_config.json`, not from prose
  - exactly one catalog entry carries `role_hint`='worker'; the Nemotron entry is demoted to candidate with its Spark validation and Thor NO-GO comments intact (cite-don't-delete)
  - tests/`test_catalog.py` passes, including its `role_hint`-uniqueness and parser-pairing guards

### t3 — Wire the recipe knobs the worker lane cannot express today

- instruction: Keep the shell-lexed STRING command form so an unset var leaves no argv token. Add only what an arm needs — moe-backend, kv-cache-dtype, attention-backend, max-num-seqs, max-num-batched-tokens, chunked-prefill, async-scheduling, prefix-caching, load-format, and an env-carried tool-call-parser (`qwen3_coder` default, `qwen3_xml` selectable). `WORKER_KV_CACHE_DTYPE` / `ATTENTION_BACKEND` / `MAX_NUM_SEQS` already render — this task is what makes them live.
- covers: c4, h3, c5, h4
- acceptance:
  - each knob added is declared in schema.py `KNOB_NAMES`, mapped in render.py, substituted in the vllm-worker compose command, documented in env.example and covered by a regenerated golden — no knob renders a key nothing reads
  - a rendered deployment with none of the new knobs set is byte-identical to today's goldens
  - the vllm-worker command does NOT pass --language-model-only

### t5 — Extend the spec-arm harness: per-position acceptance, single-stream vs aggregate

- instruction: scripts/spec-arms.py is the base; its `_LOG_LINE_RE` (:283-288) already matches the line carrying per-position rates and discards them. Reuse assess.`run_concurrent`/`auto_ramp_concurrency` for the aggregate leg rather than writing a second concurrency path.
- covers: c9, h7
- acceptance:
  - the harness captures vLLM's per-position acceptance array (its regex already sees the line) alongside the engine-wide rate, and labels engine-wide figures as engine-wide
  - single-stream decode and concurrent aggregate throughput are emitted as separately-labelled measurements in one transcript, never conflated
  - the harness is committed in-tree and re-runnable from the transcript alone

### t6 — Clear the gateway's pool-arming trap and the stale worker default

- instruction: server.py:3981-4028 raises a named ReplicaConfigError for this exact state. Prefer surfacing it as a doctor finding or a preflight rather than relaxing the guard — the guard is correct.
- covers: c13, h11
- acceptance:
  - a Thor .env that drops cortex while declaring `PRIMARY_PEER_ORIGINS` either also declares the singular `PRIMARY_PEER_ORIGIN` or is caught before the flip, not by a failed gateway boot
  - gateway/`_config.py`'s `_DEFAULT_WORKER` no longer names a checkpoint no box serves
  - the Thor's gateway boots after the flip, proving `_check_pool_arming` is satisfied

### t7 — Decide and record the pressure floor for a cortex-less Thor

- instruction: thor.toml:109 declares hand feasible=false on this card, measured. Do not flip it on a hunch; if hand is re-measured, record the measurement, otherwise document the absence.
- covers: c26, h22
- acceptance:
  - either hand is measured feasible on this card and declared so, or the shape and docs state the floor's absence explicitly as a named property
  - a pressure event on the box after the flip produces the documented behaviour — a served hand response or a 429 the docs already say to expect

### t8 — Rollout note and raw-id consumer audit, published BEFORE the flip

- instruction: docs/worker-lightning-rollout-notes.md is the precedent artifact — same shape, written for this flip. Per model-switch-playbook.md §2 no consumer here addresses the fleet by role name, so assume every consumer is a raw-id pinner until the audit proves otherwise.
- covers: c28, h24, c17, h12
- acceptance:
  - a note under docs/ names every raw-id consumer the audit found, including the acp vllm-local provider and the eidetic embed client running on this box
  - the note exists and is published before any flip step runs
  - the per-model doc lets an operator re-run the recipe from it alone: image digest, JetPack/L4T, power mode, clocks, co-resident set and the exact argv

### t10 — Back up the deployment dir and prove the rollback path

- instruction: This box's two hand-edited compose backups mean byte-for-byte restorability does NOT hold for a plain re-render; the saved .env plus the saved compose is the actual restore path. Never re-scaffold over a hand-edited compose without the backup in place.
- covers: c29, h25
- acceptance:
  - ~/.lobes is copied to a timestamped sibling before the first mutation
  - the reverse render is dry-run and shown to reproduce the pre-flip deployment before the forward flip is applied

## Wave 2

### t2 — Give associate its own catalog resolution, independent of worker

- instruction: roles.py:169 currently gives associate `ROLE_ROLE_HINT`='worker' and `_catalog_by_role_hint` (:548-550) takes the first match. Give associate its own hint or an explicit id; keep the change additive to the roles contract.
- covers: c15, h18
- acceptance:
  - a test asserts that changing the checkpoint on `role_hint`='worker' leaves the model 'associate' resolves to unchanged
  - lobes capabilities on an associate-hosting deployment still names its own checkpoint after the worker promotion

### t9 — Capture the pre-flip baseline on the Thor — the numbers that become unrecoverable

- instruction: This runs BEFORE anything is flipped — the cortex figures on this box are unrecoverable afterwards, which is exactly the trap model-switch-playbook.md records. Read-only with respect to configuration: measure, do not reconfigure.
- covers: c19, h14, c8, h6
- acceptance:
  - a transcript under docs/evidence/ records the incumbent cortex lane's single-stream decode, TTFT and prefill on this box, taken same-day with the harness the post-flip run will use
  - the transcript records power mode, clocks, co-resident set, free/tegrastats and the image digest
  - every later comparison against 61.2 tok/s names that the figure came from a different checkpoint on a different engine

## Wave 3

### t4 — Re-widen the worker role contract to multimodal coder

- instruction: Adding responsibilities is contract-compatible; removing is a break. This task only widens. The live image probe that lets capabilities ADVERTISE the widening is a separate, later task.
- covers: c33, c34, h27
- acceptance:
  - `ROLE_RESPONSIBILITIES`\['worker'\] regains `image_understanding` and `video_understanding`; `code_authoring` is removed from `ROLE_FORBIDDEN`\['worker'\]; `final_decision` and `security_decision` remain forbidden
  - no 'worker is text-only / non-coding' sentence survives in docs/colleague-stack.md, docs/worker-lightning-rollout-notes.md, docs/nemotron-3.5-lightning-30b-a3b-nvfp4.md or CLAUDE.md
  - the senses contract is untouched: `MULTIMODAL_FEASIBLE` stays false on the Thor and model=senses still resolves to the Orin peer

### t11 — Live spike on the Thor: does the checkpoint load, and which MoE backend boots

- instruction: This is the gate for everything downstream: if the checkpoint will not load, stop and report, do not substitute a different export. Drop caches before each heavy-lobe recreate and read tegrastats, never MemAvailable or docker stats. A failed boot on this box has previously leaked ~80 GiB that only a reboot cleared — treat a clean exit 0 as suspicious.
- covers: c6, h5, c23, h19, c24, h20, c25, h21
- acceptance:
  - the transcript records whether nvidia/Qwen3.6-35B-A3B-NVFP4 loads on the pinned nightly on `sm_110`, with the verbatim engine line either way
  - the moe-backend arms run auto-select first as the known-good control, then marlin, then flashinfer, each with its verbatim engine error or success line
  - `gpu_mem_util` and `max_model_len` are measured, with every refused hypothesis recorded rather than dropped
  - free/tegrastats are recorded before and after every arm and any unexplained memory gap is reported, not worked around
  - every compose invocation ran as 'env -u `GATEWAY_API_KEY` docker compose ...' and an unauthenticated probe after each gateway restart returned what the deployment intends
  - no box-level action (reboot, JetPack upgrade, driver change) was taken without the operator scheduling it, and the co-resident production stack is verified running afterwards

## Wave 4

### t12 — MTP depth sweep on the Thor: off / 1 / 3 / 5 / 7, acceptance per position

- instruction: `VLLM_GDN_DECODE_KERNEL`=triton is already set on this box and is what makes MTP reachable on `sm_110`; confirm it landed via docker inspect and capture the engine's 'GDN decode kernel: triton' line. Do not assume a larger k is faster.
- covers: c27, h23, c20, h15
- acceptance:
  - each arm's decode tok/s, TTFT, per-position acceptance and rejected draft work are recorded under identical conditions and the same harness
  - each arm's /capabilities fingerprint names the speculative config that docker inspect proves is loaded
  - vendor and forum figures appear only as named external references, never as this repo's thresholds

### t15 — Re-point the thor-worker shape at the measured checkpoint and budget

- instruction: Budget values are measured, never computed: this task consumes t11's numbers. Keep the `sm_110` moe-backend comment block current with whatever t11 actually measured.
- covers: c10, h8
- acceptance:
  - thor-worker.toml declares the nvidia checkpoint and the `gpu_mem_util` / `max_model_len` a live Thor boot measured, with every refused hypothesis recorded in its comments
  - the four tests/goldens/shapes/thor-`worker__`{base,orin,spark,thor}.env goldens are regenerated and reviewed for cross-machine bleed
  - the shape's hosts list agrees with the pressure-floor decision — no role is hosted that the card vetoes without a recorded reason

## Wave 5

### t13 — The flip: re-render the Thor, retire the cortex pool, probe the mesh

- instruction: Below the 61.2 floor is a regression and blocks acceptance. Between 61.2 and 100, accept only with the gap measured and the next lever named. Never substitute the aggregate number for the single-stream one.
- covers: c1, h1, c12, h10, c18, h13, c21, h16, h26
- acceptance:
  - model=worker answers 200 on the Thor's own gateway serving the nvidia id, with a tool call parsed into `tool_calls` (not content) and a separated reasoning trace
  - model=cortex no longer resolves locally on the Thor, and the Spark's and Orin's `PRIMARY_PEER_`\* declarations were updated in the same window — a probe from each box shows cortex still served with no 5xx and no stale `hosted_by`
  - a peer box reaches the lane as model=worker and the answer carries X-Lobes-Proxied-By
  - a live image probe with a negative control passes before capabilities advertises `image_understanding`/`video_understanding`
  - an acceptance transcript exists under docs/evidence/ recording single-stream decode against the 100 tok/s target and the 61.2 tok/s floor, with concurrent throughput reported separately

## Wave 6

### t14 — The fast gear: earn 100 tok/s or report the measured best

- instruction: The pinned nightly already registers dflash and dspark as speculative methods (probed 2026-09-10), so no custom build is needed to try it; what is unproven is `sm_110` kernel coverage. If dflash fails with a 'no kernel image' error, that is a result worth recording, not a blocker to hide.
- covers: c11, h9
- acceptance:
  - deeper MTP and DFlash are compared against the best reliable arm on the same prompts, same power mode, same harness, at concurrency 1
  - the fast gear ships as a shape or a documented env recipe over the one worker role — no new role, tier, alias or compose service appears in lobes capabilities, GET /capabilities or /v1/models
  - if no arm reaches 100 tok/s the transcript states the measured best, the arm that produced it, its distance from 100, and the specific next lever — the target is never restated as met

## End state

### Announcement

- The Thor gives up its local Qwen3.8 cortex and serves Qwen3.6-35B-A3B NVFP4 as the mesh's worker arm, on a Thor-MEASURED recipe rather than a copied one

### After state

- the Thor serves nvidia/Qwen3.6-35B-A3B-NVFP4 as 'worker' at a Thor-MEASURED budget, reached from any box as model=worker; cortex is gone from this box; the vllm-worker lane can express the recipe knobs the measurement needed (not all fifteen — the ones the arms proved matter); a docs/evidence/ transcript records single-stream decode separately from concurrent throughput and records MTP acceptance per depth; and a second, named fast profile exists only if its gain was measured and repeatable

### Success signals

- on the deployed Thor: model=worker answers 200 through the gateway with a tool call parsed into tool_calls (not content) and a separated reasoning trace; MTP is measured at k = off/1/3/5/7 with acceptance recorded per depth; and a peer box reaches the lane as model=worker with X-Lobes-Proxied-By. The THROUGHPUT TARGET is 100 tok/s single-stream decode on this box, with 61.2 tok/s (the old Thor figure) as the floor: below 61.2 is a regression and blocks acceptance; 61.2-100 is accepted only with the gap measured and the next lever named; at or above 100 the target is met. Concurrent throughput is reported separately and never substituted for the single-stream number

### Terminal tasks

- `t4` — Re-widen the worker role contract to multimodal coder
  - ROLE_RESPONSIBILITIES['worker'] regains image_understanding and video_understanding; code_authoring is removed from ROLE_FORBIDDEN['worker']; final_decision and security_decision remain forbidden
  - no 'worker is text-only / non-coding' sentence survives in docs/colleague-stack.md, docs/worker-lightning-rollout-notes.md, docs/nemotron-3.5-lightning-30b-a3b-nvfp4.md or CLAUDE.md
  - the senses contract is untouched: MULTIMODAL_FEASIBLE stays false on the Thor and model=senses still resolves to the Orin peer
- `t14` — The fast gear: earn 100 tok/s or report the measured best
  - deeper MTP and DFlash are compared against the best reliable arm on the same prompts, same power mode, same harness, at concurrency 1
  - the fast gear ships as a shape or a documented env recipe over the one worker role — no new role, tier, alias or compose service appears in lobes capabilities, GET /capabilities or /v1/models
  - if no arm reaches 100 tok/s the transcript states the measured best, the arm that produced it, its distance from 100, and the specific next lever — the target is never restated as met

### Open items

- [unknown_nonblocking] DFlash on sm_110 — the issue's worker-fast second experiment needs a vLLM build carrying both the dflash speculative method and native sm_110a support; nothing in this repo pins or has built such an image, and the cited draft (patrickbdevaney/qwen-3.6-35b-a3b-dflash-jetson-agx-thor) is an external artifact with no in-tree provenance. Deliberately downstream of the reliable arm
- [unknown_nonblocking] whether the pinned nightly's dflash kernels carry sm_110 images — the method is registered (c30) but this fleet has been bitten three separate times by kernels that exist for sm_121 and not sm_110 (GDN-MTP, LFM2 conv-hybrid, Mamba-2 SSD). Registration proves the code path, never the SASS
- [unknown_nonblocking] the checkpoint may not load at all on the pinned nightly on sm_110 — the fleet's own doc records why it serves the mmangkad copy rather than nvidia's; t11 is the gate and a NO-GO there stops the plan rather than substituting a different export
- [unknown_nonblocking] dflash is registered in the pinned image but its sm_110 kernel coverage is unproven — this fleet has been bitten three times by kernels that exist for sm_121 and not sm_110 (GDN-MTP, LFM2 conv-hybrid, Mamba-2 SSD); registration proves the code path, never the SASS
- [unknown_nonblocking] 100 tok/s may be unreachable on this board: the best figure ever measured here for this architecture is 61.2 tok/s and the only external number near the target is an unreproduced third-party DFlash claim. The honest outcome is the measured best plus the named next lever
- [out_of_scope] a JetPack 7.2 upgrade would take the co-resident Culture production stack down and is a scheduled operator event, not a plan step — if the recipe turns out to need it, the plan pauses for that scheduling rather than absorbing it
