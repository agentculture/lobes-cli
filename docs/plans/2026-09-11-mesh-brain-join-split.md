# Implementation Split Plan — mesh-brain-join

slug: `mesh-brain-join` · generated by `assign-to-workforce.sh split-plan --write` from `devague plan waves --json` (issue #82). Re-running this command overwrites this file in place; edits made to the **Task assignments** table below survive a regeneration, matched by task id.

## Dependency waves

- Wave 1: `t1`, `t2`, `t3`, `t4`, `t5`
- Wave 2: `t6`
- Wave 3: `t7`, `t10`
- Wave 4: `t8`
- Wave 5: `t9`
- Wave 6: `t11`
- Wave 7: `t12`

## Task assignments

Edit the Owner/Model columns before approving gate 2 — the default Model is a presentation-only proposal (`sonnet`), never a recommendation. Re-running `split-plan --write` preserves your edits here, matched by task id.

| Task | Owner | Model |
| --- | --- | --- |
| `t1` | qwen-code (worker lane, Thor via Spark gateway) | worker |
| `t2` | qwen-code (worker lane) | worker |
| `t3` | qwen-code (worker lane) | worker |
| `t4` | qwen-code (cortex lane) | unsloth/Qwen3.8-27B-NVFP4 |
| `t5` | qwen-code (cortex lane) | unsloth/Qwen3.8-27B-NVFP4 |
| `t6` | Claude Sonnet subagent (deviation d1: backend switch after wave 1) | sonnet |
| `t7` | Claude Sonnet subagent (deviation d1: backend switch after wave 1) | sonnet |
| `t10` | Claude Sonnet subagent (deviation d1: backend switch after wave 1) | sonnet |
| `t8` | Claude Sonnet subagent (deviation d1: backend switch after wave 1) | sonnet |
| `t9` | Claude Sonnet subagent (deviation d1: backend switch after wave 1) | sonnet |
| `t11` | Claude Sonnet subagent (deviation d1: backend switch after wave 1) | sonnet |
| `t12` | main agent (Claude) in-house: live ssh cutover with downtime | claude-fable-5-1 |

## Wave 1

### t1 — Mesh config: parse `LOBES_MESH_KEY` / `LOBES_MESH_NAME` / `LOBES_MESH_SEEDS` / `LOBES_MESH_HEARTBEAT_S` (60) / `LOBES_MESH_MISSED_MAX` (3) / `LOBES_MESH_LEDGER_PATH` into a frozen MeshConfig in a new module

- instruction: New file only. Mirror the style of `_config.py`'s `_replica_origins`/`_self_origin` parsers (verbatim, never derived). Tests in tests/`test_mesh_config.py`. Do not touch `_config.py` in this task.
- covers: c42, h33, c13
- acceptance:
  - lobes/gateway/`_mesh_config.py`: `build_mesh_config`(env) returns enabled=False with every field default when `LOBES_MESH_KEY` is unset; no other module's config output changes (existing `build_config` tests pass unchanged)
  - key set without `LOBES_MESH_NAME` raises MeshConfigError naming the missing key; name is never derived from hostname
  - seeds parse as a comma list with trailing slashes trimmed; heartbeat and missed-max reject non-positive values with a named error

### t2 — Announcement wire: schema-versioned Announcement dataclass + encode/decode in a new module, carrying name, origin, `schema_version`, and per-role RoleInfo fields plus fingerprint, capacity and a private flag

- instruction: Pure dataclasses + json, stdlib only, no I/O. Tests in tests/`test_mesh_wire.py`. The fingerprint fields must match `_replicas.py` `DISQUALIFYING_FIELDS` (`served_id`, quantization, `max_model_len`, runtime).
- covers: c5, c45, h36, c32
- acceptance:
  - lobes/gateway/`_mesh_wire.py`: decode(encode(a)) == a; unknown extra fields are ignored; a different major schema version raises MeshSchemaIncompatible with both versions in the message
  - a role marked private=True is dropped by Announcement.public() so it never leaves the box; the flag defaults to False
  - the per-role payload reuses lobes/roles.py RoleInfo fields (model/runtime/context/quant/responsibilities/`forbidden_responsibilities`) — no parallel field vocabulary

### t3 — Roster + ledger: in-memory Roster (members by name, `last_seen`, missed count, verified/unverified, flapping hold-out, name-conflict refusal, capacity clamp) and a persisted approval Ledger (name -> `approved_by`, expiry) with gossip merge, in a new module

- instruction: Pure state machine with an injected clock (like `_segmenter.py`'s style) so tests need no sleeps. Ledger persists as JSON at `LOBES_MESH_LEDGER_PATH`; write via temp file + rename. Reuse `_replicas`.`resolve_capacity`, do not copy it. Tests in tests/`test_mesh_roster.py`.
- covers: c37, h28, c9, h11, c43, h34, c53, h43, h4
- acceptance:
  - lobes/gateway/`_mesh_roster.py`: a member is dropped after `LOBES_MESH_MISSED_MAX` consecutive missed ticks, not before; a restart yields an empty roster while the ledger file is byte-identical
  - two announcements with the same name from different origins: the second is refused with `mesh_name_conflict`; same origin re-announcing updates in place
  - ledger.merge(`peer_ledger`) makes a lapsed name refused on a member that never saw the approve call; an expired entry is refused with `mesh_approval_expired` even when the join key is presented
  - announced capacity passes `resolve_capacity` (weight <= `CAPACITY_CLAMP_MAX`); a member that joins/leaves more than 3 times in 10 ticks is held out one tick with reason `mesh_flapping`; no announcement can remove another member

### t4 — Templates and scan plumbing: gateway compose passthrough for every `LOBES_MESH_`\* key, the gateway's first bind mount for the ledger file, env.example docs for the keys, `LOBES_MESH_KEY` in `scan_deployment_secrets.py`, .gitignore for the runtime ledger

- instruction: Files: docker-compose.yml, env.example, `scan_deployment_secrets.py`, .gitignore, tests/fixtures. Do NOT edit doctor.py here (t8 owns it). Follow the passthrough comment block at docker-compose.yml:2055-2058.
- covers: c8, h10, c18, h17
- acceptance:
  - lobes/templates/fleet/docker-compose.yml gateway environment: lists every key from `_mesh_config`; a rendered compose with `LOBES_MESH_LEDGER_PATH` set mounts the file read-write into the gateway
  - scripts/`scan_deployment_secrets.py` flags `LOBES_MESH_KEY`=`<value>` in a fixture under tests/fixtures/deployments and CI secrets-scan fails on it
  - tests/goldens regenerate unchanged except template-defaults.env, which gains only the new keys' defaults

### t5 — Gateway-only shape: a built-in 'gateway-only' shape (hosts=\[\]) with goldens, and relax `_routing.py`'s 'a built table always has a primary backend' assumption so a table with no local backend builds and routes only by mesh

- instruction: Shape TOML mirrors orin-small.toml's structure with hosts=\[\]. Touch `_routing.py`:432-436 only. Add tests/`test_shape_gateway_only.py`.
- covers: c6, c17, h16
- acceptance:
  - lobes/profiles/`builtin_shapes`/gateway-only.toml renders services ('gateway',) on every card and every core role FEASIBLE=false; goldens added for spark/thor/orin/base
  - `build_config` on an env with no local backend returns a RoutingTable whose `order_backends` yields nothing locally and no exception; existing tests pass
  - grep gate test: none of MESH, PEER, `SELF_ORIGIN` appear in lobes/profiles/\*.py

## Wave 2

### t6 — Mesh endpoints + heartbeat thread: GET /mesh/detect (keyless, minimal), POST /mesh/join (keyless, bounded, collapsed log), POST /mesh/announce and GET /mesh/roster and POST /mesh/approve|revoke (join-key bearer); inbound gate accepts the join key for members; heartbeat daemon thread in serve() announcing to seeds + roster members with an immediate re-announce hook

- instruction: New module lobes/gateway/`_mesh_routes.py` with the handlers; server.py gains only the route dispatch lines, the gate change (`_authorized` accepts join key), and the thread start in serve(). Follow `_tier_request.py`:223-229 for the loop and `_authlog.py` for collapsed logging. Tests with fake openers as in `test_gateway_peer_advert.py`:33-49.
- covers: c4, h7, h2, c38, h29, c39, h30, c49, h40, h12
- acceptance:
  - with `LOBES_MESH_KEY` unset: no mesh thread starts, every /mesh/\* path 404s, and the four byte-identical tests pass unchanged
  - GET /mesh/detect returns only member name, schema version and 'mesh': true — never a member list; POST /mesh/join is capped at 8 pending entries with a TTL, one per origin, and a 100-request flood logs one collapsed line
  - POST /mesh/announce without the join key is 401 and never touches the roster; with it, the member appears in GET /mesh/roster (401 without the key)
  - heartbeat thread reads its interval from MeshConfig (60 s default), uses Event.wait, and a peer whose socket hangs past the timeout delays no other peer; `reannounce_now`() is callable from the request path without blocking
  - a member with one seed learns every member listed in the seed's roster on the first tick

## Wave 3

### t7 — Membership-driven routing (core): copy-on-write routing snapshot swapped atomically from the roster; pools and auto-proxy derived from verified members (probe /capabilities must match the announcement before any forward); hand proxied like any role; realtime stays local-only; single-hop 508 preserved

- instruction: Build the snapshot in a new lobes/gateway/`_mesh_routing.py` from Roster + existing ReplicaCache/ReadinessCache probes; server.py's `_peer_only_forward`/`_proxied_owner`/`_pool_dispatch` consult the snapshot instead of table.`replica_origins`/`peer_origins`. Keep X-Lobes-Route-Reason values unchanged. Tests in tests/`test_mesh_routing.py`.
- covers: c3, h25, c7, h9, c28, h18, c44, h35, c40, h31, c14, h13
- acceptance:
  - the request path reads one immutable snapshot per request; a roster change mid-request never affects it; no lock is held across a dial
  - two fake members announcing cortex with equal fingerprints form one pool selected by `select_replica`; a member whose /capabilities probe disagrees with its announcement gets zero forwards and is marked unverified
  - model=hand on a member lacking hand forwards with X-Lobes-Proxied-By; GET /v1/realtime on a member lacking stt is 404 `role_infeasible` while another member announces stt
  - A lacking R -> B (roster says B has R) -> B lacks R too: A's request returns 508 `proxy_loop` and C is never dialled; a forwarded request carries Bearer `<join key>` and never the client's bearer
  - a member dropped from the roster leaves the pool on the next snapshot; requests for its only role 404 `role_infeasible` with no `hosted_by` within one snapshot, never hang

### t10 — CLI: 'lobes mesh status|request|approve `<name>` \[--for `<duration>`\]|revoke `<name>`' (write verbs dry-run, --apply commits) plus lobes capabilities rendering members, suffixed and private lanes

- instruction: Files: cli/`_commands`/mesh.py (new), cli/`__init__.py` (register), capabilities.py (render). Do not touch server.py. Tests in tests/`test_cli_mesh.py`.
- covers: c47, h38
- acceptance:
  - lobes/cli/`_commands`/mesh.py registered like fleet's noun; 'lobes mesh approve x' without --apply prints the plan and changes nothing; with --apply POSTs /mesh/approve with the join key
  - 'lobes mesh status' golden against a fake roster shows name, last-heartbeat age, approval expiry, verified/unverified/flapping and roles; 'lobes mesh request' works with no join key set
  - lobes capabilities shows each role's serving member and any suffixed lanes; offline fallback still renders from .env with ready=False

## Wave 4

### t8 — Naming and exposure: '{role}-{machine-name}' suffixed lanes on fingerprint disagreement, listed on /v1/models and /capabilities of every member; raw served-id on divergent lanes only 404s listing the suffixed names; private roles never exposed; immediate re-announce on lobes switch/up/unhealthy; X-Lobes-Mesh-Member on every mesh answer

- instruction: Extend `_mesh_routing.py` and the /v1/models + `capabilities_payload` builders in server.py/roles.py. The reannounce hook from t6 is triggered from the readiness cache's health transition and from lobes switch/up via a POST to the local /mesh/reannounce (join key). Tests in tests/`test_mesh_naming.py`.
- covers: c27, h1, c48, h39, h20, c46, h37, c15, h14, c16, h15
- acceptance:
  - two members announce cortex with different quantization: every member lists cortex-`<nameA>` and cortex-`<nameB>`; model=cortex returns 404 `role_infeasible` with `hosted_by` empty, never one of them
  - a raw id hosted only as suffixed lanes 404s with a body listing every suffixed name; hosted locally it is served locally
  - a role announced private is absent from a peer's roster, /v1/models and /capabilities and still answers locally
  - changing a fake member's quantization moves it from the plain pool to a suffixed lane within one probe refresh (5 s), not one heartbeat; X-Lobes-Route-Reason set unchanged (assert against `_selection.py`'s constant); X-Lobes-Mesh-Member present on pooled, proxied and suffixed answers

## Wave 5

### t9 — Retire the \*`_PEER_`\* family in code + doctor: delete `PEER_ORIGIN`/ORIGINS/PROXY/`API_KEY`(S) parsing and ReplicaConfigError paths from `_config.py`/`_routing.py`; doctor gains three findings: leftover \*`_PEER_`\* keys, shell `LOBES_MESH_KEY` differing from .env, and mesh passthrough coverage

- instruction: Cite-don't-delete applies to docs (t10), not to dead parsing code: remove it. Update tests/`test_gateway_proxy.py`, `test_gateway_pool.py`, `test_gateway_config_proxy.py`, `test_gateway_peer_only_pool.py` to build peers through fake roster announcements instead of env keys. doctor.py is owned by this task alone.
- covers: c29, h19, c50, h41
- acceptance:
  - grep of lobes/ for `PEER_ORIGIN`, `PEER_PROXY`, `PEER_API_KEY` returns only 'Retired' comments; the four byte-identical tests and every existing gateway test pass (rewritten where they declared peers)
  - lobes doctor on an .env carrying `PRIMARY_PEER_ORIGIN` reports a named finding `peer_family_retired` listing each key; on a clean .env reports nothing
  - lobes doctor reports `mesh_key_shell_mismatch` when the invoking shell's `LOBES_MESH_KEY` differs from .env, and `passthrough_missing` when a `LOBES_MESH_`\* key is absent from the compose gateway block

## Wave 6

### t11 — Docs and catalog follow the replacement: gateway-fleet.md, deployment-shapes.md, colleague-stack.md, openai-api.md, secret-rotation.md (join-key rotation = fleet-wide restart), env.example, lobes explain, CLAUDE.md; old peer sections moved under 'Retired'; deployments/jetson-agx-`thor__thor`-worker re-captured; version bump

- instruction: Use the doc-test-alignment checklist. Re-capture the Thor entry from the repo (no CLI verb exists). Bump with python3 .claude/skills/version-bump/scripts/bump.py minor (pipe the changelog JSON on stdin).
- covers: c41, h32
- acceptance:
  - grep of docs/ and lobes/explain for `PEER_ORIGIN`|`PEER_PROXY`|`PEER_API_KEY`|`PEER_ORIGINS` outside a '## Retired' heading returns nothing; markdownlint passes
  - deployments/jetson-agx-`thor__thor`-worker/ re-captured via lobes.runtime.`_lock`.`capture_lock` against the new template; secrets-scan passes; VARIATION.md unchanged in its Measured result section
  - pyproject version bumped minor with a CHANGELOG entry naming the retired family and the new keys

## Wave 7

### t12 — Live cutover and validation on Spark + Thor + Orin: baseline capture, per-box backups, migrate all three onto the join key with `LOBES_MESH_NAME`/SEEDS, boot the gateway-only shape on the Orin as a fourth-member test, measure the five success signals, file the evidence transcript

- instruction: ssh thor@thor / ssh orin@orin; on the Thor run every compose command under env -u `GATEWAY_API_KEY`; on the Spark always pass all four -f compose files; install the lobes CLI on Thor and Orin first (uv tool install) and fix the Orin `MODEL_GEAR_VERSION` pin. Downtime approved. Follow docs/model-switch-playbook.md's evidence format.
- covers: c1, h5, c2, h6, c33, h21, c34, h22, c35, h23, c36, h24, c52, h42, h8
- acceptance:
  - docs/evidence/2026-MM-DD-baseline-mesh-cutover.txt captures each box's redacted \*`_PEER_`\* block and the stale Orin pool entry BEFORE any change; ~/.lobes.pre-mesh-`<UTC>` exists on every box and is named in the transcript
  - after cutover: zero \*`_PEER_`\* keys on any box (lobes doctor 0 findings on all three); a member removed and re-added with the key alone is listed by all others within <= 120 s; no other member's ~/.lobes changed (diff shown)
  - 4 concurrent model=cortex requests to one member are served by two compatible members (both X-Lobes-Served-By and X-Lobes-Proxied-By observed); model=hand and model=cortex from the Thor answer 200 by proxy; Qwen Code reaches a role through the local gateway
  - stopping one member's gateway: others answer 200 for their roles within <= 120 s and 404 `role_infeasible` in < 10 s for a role only it had; with `LOBES_MESH_KEY` removed on one box its responses are byte-identical to 0.75.x
  - the gateway-only shape boots on one box and serves model=cortex by proxy; each success signal (1)-(5) has its measured value and PASS/FAIL, failures recorded not dropped

## End state

### Announcement

- lobes mesh-brain: a home fleet of robots and boxes running lobes-cli forms one brain. Any machine with a key, or an operator approval (timed or permanent), joins the mesh at runtime, announces the roles, models and purpose it serves, and can draw on every lobe in the mesh even if it serves nothing itself. A configurable heartbeat (default once a minute) keeps it connected. Two machines serving the same role share one concurrency pool out of the box, so a second Spark joining Spark+Thor+Orin doubles the cortex pool or brings a new model.

### After state

- A new box runs 'lobes init --apply' with the mesh join key in its .env and 'lobes serve --apply'; within one heartbeat interval every other member lists its roles on /v1/models and /capabilities, same-role compatible lanes pool automatically, differing lanes appear as '{role}-{machine-name}', and roles the new box lacks are reachable on its own gateway by proxy. Removing the box (or the key) makes it vanish from every roster after missed heartbeats and the mesh keeps serving with what remains.

### Success signals

- Measured on the live Spark+Thor+Orin fleet after cutover: (1) a member joining with the key is listed by all other members within <= 2 heartbeat intervals (<= 120 s at the 60 s default); (2) zero *_PEER_* keys remain in any member's .env and lobes doctor reports 0 findings; (3) with 2 members serving a compatible cortex, 4 concurrent requests to one member's gateway are served by both (X-Lobes-Served-By and X-Lobes-Proxied-By both observed); (4) stopping one member's gateway: the others answer 200 for the roles they still hold within <= 2 intervals, and 404 role_infeasible (never a hang > 10 s) for a role only it had; (5) with no join key set, the gateway's responses are byte-identical to 0.75.x (existing byte-identical tests still pass).

### Terminal tasks

- `t4` — Templates and scan plumbing: gateway compose passthrough for every LOBES_MESH_* key, the gateway's first bind mount for the ledger file, env.example docs for the keys, LOBES_MESH_KEY in scan_deployment_secrets.py, .gitignore for the runtime ledger
  - lobes/templates/fleet/docker-compose.yml gateway environment: lists every key from _mesh_config; a rendered compose with LOBES_MESH_LEDGER_PATH set mounts the file read-write into the gateway
  - scripts/scan_deployment_secrets.py flags LOBES_MESH_KEY=<value> in a fixture under tests/fixtures/deployments and CI secrets-scan fails on it
  - tests/goldens regenerate unchanged except template-defaults.env, which gains only the new keys' defaults
- `t12` — Live cutover and validation on Spark + Thor + Orin: baseline capture, per-box backups, migrate all three onto the join key with LOBES_MESH_NAME/SEEDS, boot the gateway-only shape on the Orin as a fourth-member test, measure the five success signals, file the evidence transcript
  - docs/evidence/2026-MM-DD-baseline-mesh-cutover.txt captures each box's redacted *_PEER_* block and the stale Orin pool entry BEFORE any change; ~/.lobes.pre-mesh-<UTC> exists on every box and is named in the transcript
  - after cutover: zero *_PEER_* keys on any box (lobes doctor 0 findings on all three); a member removed and re-added with the key alone is listed by all others within <= 120 s; no other member's ~/.lobes changed (diff shown)
  - 4 concurrent model=cortex requests to one member are served by two compatible members (both X-Lobes-Served-By and X-Lobes-Proxied-By observed); model=hand and model=cortex from the Thor answer 200 by proxy; Qwen Code reaches a role through the local gateway
  - stopping one member's gateway: others answer 200 for their roles within <= 120 s and 404 role_infeasible in < 10 s for a role only it had; with LOBES_MESH_KEY removed on one box its responses are byte-identical to 0.75.x
  - the gateway-only shape boots on one box and serves model=cortex by proxy; each success signal (1)-(5) has its measured value and PASS/FAIL, failures recorded not dropped

### Open items

- [unknown_nonblocking] Service-rate weighting so a heterogeneous pool does not slow down under load (issue #232): PeerReplica.weight exists but no channel populates it. Not needed to form the pool; needed before any throughput claim.
- [follow_up] Fleet hygiene before live validation: Orin MODEL_GEAR_VERSION pin drift (0.63.1 vs 0.70.0 served), missing GATEWAY_SELF_ORIGIN on Orin, lobes CLI absent on Thor and Orin, stale Thor entry in Orin's cortex pool.
- [follow_up] Raw-id requests under local pressure are not forwarded (#215, pressure gate is tier-alias-only); an auto-formed pool inherits this divergence until #215 lands. (Re-parked: v4 was resolved by mistake in place of the hygiene park v3.)
- [follow_up] LAN-local zero-config discovery (mDNS) for robots on the same Wi-Fi with no tailnet — seeds suffice for this fleet; revisit when a box joins without a tailnet address.
- [unknown_nonblocking] Join-key rotation without a fleet-wide restart (e.g. accepting old+new for one interval).
- [unknown_nonblocking] Heterogeneous same-role pools balance by slot count, not service rate (#232): the Spark/Thor cortex pool may serve slower under load than pinning; success signal (3) checks placement, not throughput.
- [unknown_nonblocking] The gateway-only shape has never booted on a physical Orin (#108); t12's fourth-member test is its first live run and may surface a routing assumption t5 did not relax.
- [follow_up] Raw-id requests under local pressure are not forwarded (#215); the mesh pool inherits that until #215 lands — a follow-up, not part of this plan.
- [unknown_nonblocking] t6, t7, t8 and t9 all edit server.py and must be built serially (they are chained by dependencies); do not widen those waves.
- [follow_up] Cutover downtime: consumers mid-request during a gateway recreate see connection errors; approved by the operator, to be scheduled in a quiet window and noted in the transcript.
