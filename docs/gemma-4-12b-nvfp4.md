# Gemma 4 12B (unified multimodal, NVFP4 + native MTP)

`sakamakismile/gemma-4-12B-coder-fable5-composer2.5-MTP-NVFP4` — the fleet's
`multimodal` generate gear (the new `normal` tier): a unified Gemma 4 12B serving
text + image + audio in one checkpoint, NVFP4-quantized with a native MTP draft
head for vLLM speculative decoding.

> Stub — full content (serve flags, load-test numbers, the confirmed native-MTP
> `--speculative-config`, and the measured native context) is added in t9.
